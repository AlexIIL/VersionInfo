repositories {
    mavenCentral()
}

apply plugin: 'java'
apply plugin: 'eclipse'

dependencies {
    compile 'com.google.code.gson:gson:2.3.1'
}

jar
{
    manifest {
        attributes 'main-class': 'alexiil.utils.version.VersionInfo'
    }
}
